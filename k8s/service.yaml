apiVersion: v1
kind: Service
metadata:
  name: auth-service-node
  labels:
    app: auth-service-node
spec:
  type: ClusterIP
  selector:
    app: auth-service-node
  ports:
    - protocol: TCP
      port: 3001
      targetPort: 3001
